# MANAGED BY PUPPET
<% exports.each do |export,config| -%>
# <%= export %>
 <% config['allowed_clients'].each do |client,opt| -%>
  <%= config['export_point'] -%> <%= client -%>(<%= opt.join(",") -%>)
 <% end -%>
<% end -%>

